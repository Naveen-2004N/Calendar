import React from 'react'; import { WeekView } from '../components/WeekView'; import { sampleEvents } from '../sample-data'; import { startOfWeek } from '../utils/date'; export default { title: 'Calendar/WeekView' }; export const Default = () => <div style={{width:1100}}><WeekView startOfWeek={startOfWeek(new Date())} events={sampleEvents} onEventClick={()=>{}} onCreate={()=>{}} onMove={()=>{}} onResize={()=>{}} /></div>;